{
  "name": "run",
  "scripts": {
    "build": "zip -r node_modules.zip node_modules/",
    "deploy": "stitch-cli import --strategy=replace --include-dependencies"
  },
  "engines": {
    "node": "8"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "private": true
}
